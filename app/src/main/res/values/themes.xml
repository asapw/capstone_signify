<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light Theme Customization -->
    <style name="Base.Theme.MyCapstone.Light" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primaryColorLight</item>
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="android:windowBackground">@color/light_background</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:colorControlNormal">@color/primaryColorLight</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="colorButtonNormal">@color/secondaryColor</item>
        <item name="backgroundColor">@color/backgroundLight</item>
        <item name="itemBackground">@color/backgroundItemLight</item>
    </style>

    <style name="Theme.MyCapstone" parent="Base.Theme.MyCapstone.Light" />
</resources>
